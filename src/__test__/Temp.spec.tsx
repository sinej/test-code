describe('it, test의 묶음', () => {
    beforeEach(() => {
        console.log("beforeEach")
    })
    it('should test the first case', () => {
        console.log("it")
    });
    test('테스트케이스 1번', () => {
        console.log("test")
    });
});